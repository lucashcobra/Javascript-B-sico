const frutas = [" Pera " , " Laranja"]
        frutas.unshift("Mexerica ") // o unshift coloca um novo item/elemento no começo do array. 
        // frutas.shift() remove o primeiro item/elemento do array.
        document.write(frutas)

        const frase = "Javascript é fantástico!" 
        console.log(frase.startsWith("Java")) // mostra no console se a string começa com a palavra que coloquei, respondendo true or false. 
        console.log(frase.endsWith("!")) // mostra se a string termina com a palavra que coloquei, respondendo true or false.